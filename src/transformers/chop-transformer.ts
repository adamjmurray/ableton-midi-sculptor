import Transformer from './transformer'
import Note from '../note'
// import { log } from '../logger'

export default class ChopTransformer extends Transformer {
  
  set notes(_notes: Note[]) {
    
  }
}
